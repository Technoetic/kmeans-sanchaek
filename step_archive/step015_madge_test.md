# Step 015 - madge 환경 검증

- madge 버전: 8.0.0
- 동작 확인: `npx madge --version` 정상 응답
- 본 프로젝트는 정적 SPA(HTML + 단일 JS 모듈)이므로 madge는 step048 산출물 검증에서 1회 실행 예정.

## @MX 태그 의무 진입

step015 이후 모든 신규 소스에 @MX:NOTE 최소 1개 첨부.
`mx-tag-validator.ps1`는 PostToolUse(Write/Edit)에서 fail-open으로 경고만 출력.

## Self-Calibration

- 목표 달성: Y
